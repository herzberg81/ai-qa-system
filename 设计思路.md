# 一、软件概述

## 1.1 软件名称

AI 基础问答系统

## 1.2 开发目标

开发一款轻量化人工智能基础知识问答桌面工具，基于字典、集合等基础数据结构实现知识库存储与关键词匹配检索，搭配可视化 GUI 完成人机交互；完整实现知识库构建、关键词分词匹配、用户提问日志记录、循环问答交互功能，同时使用 Git 完成版本管控并开源至代码托管平台。

## 1.3 开发环境

- 编程语言：Python 3.10 及以上
- GUI 框架：Tkinter（Python 内置，无需额外安装）
- 版本管理工具：Git
- 代码托管平台：Gitee / GitHub
- 编辑器：VS Code

## 1.4 软件核心评分需求对应说明

- 构建 15~20 条 AI 基础问答知识库，字典存储问答对、集合存储关键词、基于关键词分类优化检索；
- 可视化交互界面，支持循环提问、输入 "退出" 结束程序，列表持久记录全部用户提问；
- 集合交集关键词匹配算法，按匹配关键词数量择优返回答案，无匹配时给出统一提示；
- Git 版本迭代管理，项目代码上传至代码仓库，提供公开访问链接。

---

# 二、总体架构设计

## 2.1 项目目录结构

```plaintext
ai_qa_system/
├── main.py               # 程序入口，GUI主界面、交互主循环
├── knowledge_base.py     # 知识库构建、关键词提取、分类封装函数
├── match_engine.py       # 关键词匹配核心算法模块
├── record_log.py         # 用户提问记录存储、读取工具函数
├── utils.py              # 通用工具（分词、集合处理工具）
├── README.md             # 项目使用说明、仓库地址、运行教程
└── .gitignore            # Git忽略文件配置（日志缓存、pyc文件等）
```

## 2.2 分层设计思想

整体分为四层，低耦合分层开发，单一职责便于修改与维护：

- 数据层：负责知识库存储、关键词集合、用户提问日志列表维护；
- 算法层：封装关键词提取、集合交集匹配、最优问题筛选逻辑；
- 交互层：Tkinter 可视化界面，输入框、输出文本、按钮、退出逻辑；
- 版本管理层：Git 分支管理、提交记录、远程仓库推送。

## 2.3 整体业务流程

启动程序 → 自动加载内置知识库，提取关键词并分类 → 弹出可视化交互窗口 → 用户输入问题提交查询 → 工具提取用户问题关键词集合 → 与所有知识库关键词集合求交集，计算匹配数量 → 筛选交集最多的问答输出答案，无匹配则提示无结果 → 将本次提问存入日志列表 → 等待下一次提问；输入 "退出" 则关闭界面、保存提问记录、程序结束。

---

# 三、核心模块详细设计

## 3.1 模块 1：知识库构建模块（knowledge_base.py）

### 3.1.1 数据结构设计

**问答字典 kb_dict**

- 键：完整问题字符串；值：对应标准答案；
- 容量：预设 15~20 条人工智能基础问答，覆盖 AI 定义、机器学习、深度学习、Python、神经网络、数据集、训练、过拟合等基础知识点。

示例：

```python
kb_dict = {
    "什么是人工智能？": "人工智能是让机器模拟人类感知、推理、学习的技术统称...",
    "机器学习和深度学习的区别？": "深度学习属于机器学习分支，依靠多层神经网络自动提取特征..."
}
```

**全局关键词集合 all_keywords_set**

- 作用：提取每条问题的核心词汇，存入集合自动去重，用于全局检索参考；
- 提取规则：过滤助词、标点，仅保留名词、专业术语（人工智能、机器学习、神经网络、损失函数等）。

**单条问题关键词映射 kw_map**

- 数据结构：字典 {完整问题: {关键词集合}}；
- 用途：存储每条问题专属关键词集合，匹配时直接取集合求交集；

**关键词分类字典 classify_kb**

- 结构：{关键词: [包含该关键词的所有问题列表]}；
- 优化目的：检索时可先按关键词过滤候选问题，减少匹配计算量，提升查询效率。

### 3.1.2 核心函数设计

- `build_knowledge_base()`：初始化 15~20 条问答字典，返回原始知识库；
- `extract_keywords(sentence)`：分词工具，输入文本，输出关键词集合；
- `build_keyword_mapping(kb)`：遍历知识库，生成每条问题关键词集合、全局关键词集合、关键词分类字典；

模块初始化自动执行，程序启动一次性加载完成，无需重复计算。

## 3.2 模块 2：关键词匹配引擎（match_engine.py）

### 3.2.1 匹配算法设计（集合交集核心逻辑）

1. 获取用户输入问题，调用分词函数生成用户关键词集合 user_kw；
2. 遍历知识库所有问题对应的关键词集合，分别计算 user_kw & 问题关键词集合，得到交集；
3. 记录每条问题交集元素数量，筛选交集数量最大值对应的问题；
4. 分支判断：
   - 最大交集数量 > 0：返回该问题对应的标准答案；
   - 最大交集数量 = 0：返回固定提示语：抱歉，未找到相关答案，请尝试其他问题。

### 3.2.2 核心函数

`match_answer(user_input, kw_map)`：

- 入参：用户输入字符串、知识库关键词映射字典；
- 返回：匹配结果文本。

## 3.3 模块 3：用户交互与日志记录（main.py + record_log.py）

### 3.3.1 GUI 可视化界面设计

使用 Tkinter 搭建窗口，包含以下组件：

- 文本提示标签：请输入人工智能相关问题，输入【退出】结束问答；
- 单行输入框：接收用户输入问题；
- 提交按钮：点击触发匹配查询；
- 多行只读文本框：展示问答对话记录；
- 退出按钮：手动关闭程序。

### 3.3.2 循环交互逻辑

1. 窗口持续监听用户输入，每次点击提交执行一次匹配；
2. 读取输入框内容，去除首尾空格；
3. 判断输入内容是否等于 "退出"：
   - 是：保存全部提问日志，关闭窗口，程序终止；
   - 否：执行匹配函数，将用户问题 + 系统答案追加至对话文本框。

### 3.3.3 提问日志存储设计

- 全局列表 question_record_list：顺序存储用户每一次完整提问；
- 工具函数 save_record(record_list)：程序退出时将列表内所有提问写入本地 txt 文件，留存历史提问；
- 每次提问完成自动 append 到列表，保证记录不丢失。

## 3.4 模块 4：通用工具模块（utils.py）

封装复用工具函数，降低代码冗余：

- 标点过滤函数：清除中文、英文标点符号；
- 停用词过滤：去除 "什么、是、和、的" 等无意义助词；
- 集合交集数量统计工具。

---

# 四、数据结构设计说明

**字典（知识库、关键词分类）**

- 优势：键值对一一对应，可快速通过问题查找答案、通过关键词筛选对应问题，查询效率高；

**集合（关键词存储）**

- 优势：自动去重，内置交集运算，一行代码即可完成关键词重合度计算，简化匹配算法；

**列表（用户提问日志）**

- 优势：有序存储，支持追加、遍历，完整保留用户提问先后顺序，便于日志持久化存储。

---

# 五、版本管理设计（Git）

## 5.1 分支规划

- main 主分支：存放稳定可运行完整版本，最终提交开源；
- dev 开发分支：日常迭代开发、功能调试；
- 功能分支：feature/kb 知识库开发、feature/match 匹配算法开发、feature/gui 界面开发，开发完成合并至 dev。

## 5.2 迭代提交规划

1. 第一次提交：搭建项目目录，编写基础知识库；
2. 第二次提交：完成关键词提取、集合匹配算法；
3. 第三次提交：开发 Tkinter 可视化交互界面；
4. 第四次提交：新增用户提问日志记录功能；
5. 第五次提交：优化关键词分类检索逻辑，完善注释与 README；
6. 最终提交：整合全部功能，推送至远程仓库。

## 5.3 远程仓库规范

- 仓库名称：ai_basic_qa_system
- 仓库公开权限：公开（Public），提供可直接访问 URL；
- 仓库包含内容：全部源代码、README 使用文档、.gitignore 配置；
- README 写明运行方式、内置知识库清单、功能介绍、Git 提交记录说明。

---

# 六、软件运行流程

1. 运行 main.py，程序自动加载知识库并预计算所有关键词集合；
2. 弹出可视化问答窗口；
3. 用户在输入框输入 AI 相关问题，点击提交；
4. 系统提取用户问题关键词集合，与知识库所有问题关键词求交集；
5. 匹配最优问题并输出答案，同时将本次提问存入日志列表，对话实时展示在文本框；
6. 重复提问，直至用户输入 "退出"；
7. 程序自动保存所有提问记录至本地日志文件，关闭窗口结束运行。

---

# 七、异常处理设计

- 用户输入空内容：弹窗提示 "请输入有效问题"，不执行匹配；
- 关键词全部无交集：统一返回未匹配提示；
- 知识库加载异常：捕获异常并弹窗提示知识库初始化失败；
- 文件写入日志失败：控制台打印报错，不中断主程序交互。

---

# 八、软件设计亮点

1. 充分利用字典、集合、列表三种基础数据结构完成业务逻辑，贴合课程实操要求；
2. 关键词集合交集匹配算法逻辑简洁，检索效率优于简单字符串包含匹配；
3. 可视化 GUI 交互，操作直观，支持连续问答，完整记录用户提问行为；
4. 模块化拆分代码，知识库、匹配算法、界面、日志完全解耦，易于扩充知识库条目；
5. 标准化 Git 版本管理，完整迭代记录，代码开源可复用。

---

# 九、拓展预留方案

- 可扩充知识库条目，无需修改匹配算法；
- 可新增权重机制，为高频关键词设置更高匹配优先级；
- 可增加模糊分词、同义词库，提升匹配准确度；
- 可增加日志统计功能，统计用户高频提问。
